#include "lists.h"

/**
* check_cycle - checks if a linked list contains a cyce
* @list: linked list to check
*
* Return: 1 if the list has a cycle, 0 if it doesn't
*/
int check_cycle(listint_t *list)
{
